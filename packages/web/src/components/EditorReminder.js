import React from 'react';

const EditorReminder = () => {

  return (
    <React.Fragment>
      <div>This is a editorReminder component.</div>
    </React.Fragment>
  );
};

export default React.memo(EditorReminder);
