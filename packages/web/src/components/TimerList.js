import React from 'react';

const TimerList = () => {

  return (
    <React.Fragment>
      <div>This is a timerList component.</div>
    </React.Fragment>
  );
};

export default React.memo(TimerList);
