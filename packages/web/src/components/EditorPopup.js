import React from 'react';

const EditorPopup = () => {

  return (
    <React.Fragment>
      <div>This is a editorPopup component.</div>
    </React.Fragment>
  );
};

export default React.memo(EditorPopup);
