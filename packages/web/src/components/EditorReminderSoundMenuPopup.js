import React from 'react';

const EditorReminderSoundMenuPopup = () => {

  return (
    <React.Fragment>
      <div>This is a editorReminderSoundMenuPopup component.</div>
    </React.Fragment>
  );
};

export default React.memo(EditorReminderSoundMenuPopup);
