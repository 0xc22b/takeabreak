import React from 'react';

const TimerItem = () => {

  return (
    <React.Fragment>
      <div>This is a timerItem component.</div>
    </React.Fragment>
  );
};

export default React.memo(TimerItem);
