import React from 'react';

const TimerItemMenuPopup = () => {

  return (
    <React.Fragment>
      <div>This is a timerItemMenuPopup component.</div>
    </React.Fragment>
  );
};

export default React.memo(TimerItemMenuPopup);
