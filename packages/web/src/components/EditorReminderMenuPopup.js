import React from 'react';

const EditorReminderMenuPopup = () => {

  return (
    <React.Fragment>
      <div>This is a editorReminderMenuPopup component.</div>
    </React.Fragment>
  );
};

export default React.memo(EditorReminderMenuPopup);
