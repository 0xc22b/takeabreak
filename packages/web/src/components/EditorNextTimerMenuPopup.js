import React from 'react';

const EditorNextTimerMenuPopup = () => {

  return (
    <React.Fragment>
      <div>This is a editorNextTimerMenuPopup component.</div>
    </React.Fragment>
  );
};

export default React.memo(EditorNextTimerMenuPopup);
