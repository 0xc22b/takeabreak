import React from 'react';

const EditorNextTimerStartsByMenuPopup = () => {

  return (
    <React.Fragment>
      <div>This is a editorNextTimerStartsByMenuPopup component.</div>
    </React.Fragment>
  );
};

export default React.memo(EditorNextTimerStartsByMenuPopup);
